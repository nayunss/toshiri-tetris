# 푸시 대상에서 제외 (사용자 지정, 2026-09-14)
# 전부 로컬에는 남긴다. 지우지 말 것.

# 요구사항 원문
prompt.md

# 중간 산출물 — 스펙·QA 리포트·검증 스크린샷 (사후 진단의 근거)
_workspace/

# 프롬프트가 참조하는 원본 에셋.
# 앞에 "/"가 붙어 이 폴더 바로 아래만 가리킨다 — public/icon/은 빌드가 쓰므로 제외하면 안 된다.
/icon/
/reference/

# 독립 레포라 상위 .gitignore를 물려받지 않는다 — 여기에 직접 적는다
node_modules/
dist/
.DS_Store
*.log
*.tsbuildinfo
